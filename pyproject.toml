[project]
name = "enterprise-identity-graph"
version = "0.1.0"
description = "A Python identity graph engine for enterprise IAM and privilege risk analysis."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{name = "Rafi's Projects"}]
dependencies = []

[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-q"
testpaths = ["tests"]
